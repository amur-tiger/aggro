{
  "name": "@aggro/aggro",
  "version": "1.0.0",
  "description": "Aggregator",
  "license": "MIT",
  "author": "Arne 'TigeR' Linck",
  "keywords": [
    "Feed",
    "Aggregator"
  ],
  "scripts": {
    "build": "pnpm build --recursive",
    "start": "concurrently --kill-others \"docker-compose up database\" \"npm:watch\"",
    "watch": "pnpm watch --recursive --stream",
    "test": "pnpm test --recursive"
  },
  "devDependencies": {
    "concurrently": "^7.1.0",
    "nodemon": "^2.0.15",
    "prettier": "^2.6.0",
    "prettier-plugin-svelte": "^2.6.0",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.2"
  }
}
